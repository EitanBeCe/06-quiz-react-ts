App description...
useCallback
effects
refs

Added custom hooks:

- useOnMount (useEffect with empty deps array []. Similar to 'viewDidSet')
- useOnChange (useEffect with other syntax, to see what you want to react exactly. And what are other additional deps)
